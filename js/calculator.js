const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

const calculator = {
	add,
	subtract,
	multiply,
	divide,
};
calculator.add(3, 5);
export default calculator;
